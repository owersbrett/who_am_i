import 'package:flutter/material.dart';

class QualityCard extends StatelessWidget {
  const QualityCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
